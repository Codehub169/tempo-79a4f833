### Codehub Execution Engine Environment Variables

# Database URL for SQLite. This will create/use 'database.db' inside the 'app' directory.
DATABASE_URL=sqlite:///./app/database.db

# Docker host. Use 'unix:///var/run/docker.sock' for local Docker daemon on Linux,
# or 'tcp://localhost:2375' for Docker Desktop (check your Docker settings for the correct host).
# Ensure your user has permissions to access the Docker socket if using unix socket.
DOCKER_HOST=unix:///var/run/docker.sock

# Host for the FastAPI application. Use 0.0.0.0 to make it accessible from outside the container.
API_HOST=0.0.0.0

# Port for the FastAPI application. This must be 9000 as per project requirements.
API_PORT=9000
